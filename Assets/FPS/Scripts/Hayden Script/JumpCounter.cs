using System.Collections;
using System.Collections.Generic;
using Unity.VisualScripting;
using UnityEngine;

//public float Jump
//The float jump will change based on how many times that the player has pressed the space bar
public class JumpCounter : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        //This is obviously the wrong typing of it but the general idea of it should be understood
        //if (OnButtonClick "Space") ;
        //{
        // ++Jump
        //}
        
    }
}
